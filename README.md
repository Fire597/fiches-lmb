# Fiches élèves LMB
Par Hugo Feutry et Hugo Cartigny.

- [x] Une fois le projet terminé, ajouter du php :

```php
$req = $bdd->prepare(...);
$req-execute(array(...));

while ($ans = $req->fetch()) {
    $saved_data = $ans['prenom'];
    ...
}
```
- [x] Anything else ? :)